import React from "react";
import {TableInterface} from "@/container-interfaces/table-interface";

export const TableContainer = ({containerName, columns, rows, backgroundColor}: TableInterface) => {

    return (
        <div>



        </div>
    )
}